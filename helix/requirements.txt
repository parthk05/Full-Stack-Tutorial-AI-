# Step 1.1 — FastAPI shell + local server
fastapi==0.116.1
uvicorn[standard]==0.35.0

# Step 1.1 / 2.1 — load LLM_API_Key + MODEL_NAME from .env without hardcoding secrets
python-dotenv==1.1.1
pydantic-settings==2.10.1

# Step 2.1 — Google Gemini provider SDK (matches MODEL_NAME=gemini-*)
google-genai==1.28.0
